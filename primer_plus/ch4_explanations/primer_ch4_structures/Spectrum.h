#pragma once

// Enumeration data structure
enum Spectrum {
	red,
	orange,
	yellow,
	green,
	blue,
	violet,
	indigo,
	untraviolet
};

enum Bits {
	one = 1,
	two = 2,
	four = 4,
	eight = 8
};

enum Bigstep {
	first,			// 0
	second = 100,	// 100
	third			// 101
};

enum {
	zero,			// 0
	null = 0,		// 0
	one,			// 1
	numeroUno = 1	// 1
};
